# 조사 돌리는 법

Codex goal 모드에 아래 프롬프트를 그대로 넣는다. 한 달 이상 혼자 돈다.

숫자는 `logs/state.json` 이 원장이다. 이 문서의 숫자는 프롬프트를 넣던 날(2026-08-11) 기준이다.

---

## goal 모드에 넣을 프롬프트

```
C:\dev\effect-pool 에서 근거 항목을 계속 늘린다.

이 목록은 "무엇을 했을 때 얼마나 달라지나"에 답하는 자료다. 개입군과 대조군이
얼마나 갈렸는지 숫자로 적을 수 있으면 항목이 된다. 허가 여부는 상관없다.

이 목표는 끝나지 않는다. 완료로 처리하지 마라. 보고하고 멈추지 마라.
한 바퀴가 끝나면 곧바로 다음 바퀴를 시작한다. 내가 멈추라고 할 때까지 반복한다.
사람한테 물어보지 마라. 막히면 아래 "막혔을 때"를 따르고 계속 간다.

시작할 때 git pull 하고 AGENTS.md 를 읽어라. 규칙 2.5(갈래)와 3.55(얼마나 갈렸나)가
새로 생겼다. 그 둘을 안 지키면 빌드가 선다.

지금 상태 (2026-08-11): 항목 4,970건, 주어 2,059종.
갈래는 처방약 1,873 · 생활마음 1,153 · 시술기기 634 · 운동 583 · 음식 408 ·
보충제 276 · 약국약 40 이다. 처방약 말고 다른 쪽을 늘리는 게 이번 목표다.
Vercel 을 GitHub 저장소에 연결해뒀다. 푸시하면 알아서 빌드해서 올라간다.


## 한 바퀴

1. 남은 일감을 센다.
   PowerShell 이면 (Select-String -Path data/worklist.md -Pattern '^- \[ \]').Count
   30개 밑이면 6번을 먼저 하고 온다.

2. 미처리 주제 30개를 고른다. 고르는 기준이 이번 목표의 전부다.

   30개 중 최소 20개를 약국약·보충제·음식·운동·생활·시술기기에서 고른다.
   처방약은 10개까지만이다. 빌드가 "약국약·보충제·음식이 4분의 1이 안 된다"고
   경고하는 동안에는 그 세 갈래를 우선한다.

   허가 적응증이라고 건너뛰지 마라. 옛 규칙이었고 지금은 아니다.
   커피가 간암 위험을 얼마나 낮추는지, 미녹시딜이 머리를 얼마나 나게 하는지는
   허가와 상관없이 사람들이 알고 싶어 하는 숫자다.

   worklist 에 "옛 규칙이 허가 적응증이라고 뺐던 줄" 주석이 붙은 2,949줄이 있다.
   시험 수와 논문 수를 이미 확인해둔 줄이라 새로 뽑는 것보다 낫다. 여기서 먼저 골라라.

3. AGENTS.md 규칙 3의 순서대로 조사해서 항목으로 만든다. 새로 지킬 것 셋.

   - vs — 무엇을 재서 개입군 얼마, 대조군 얼마, 차이 얼마. 숫자가 없으면 빌드가 선다.
     대조군이 없으면 "대조군 없는 단일군. 전 32.1점 → 후 24.8점" 이라고 적는다.
   - 주어를 한국어로 쓴다. 소문자 영문이 4글자 넘게 들어가면 빌드가 선다.
     대문자 약어(EGCG·HMB·CPAP)는 그대로 둔다.
   - 새 주어는 data/kinds.json 에 갈래를 한 줄 적는다. 없으면 빌드가 선다.

4. node build.mjs 를 돌린다.
   새 항목에서 난 오류는 그 자리에서 전부 고친다. 옛 항목 일감도 이 바퀴에 같이 던다.

   - 옛 항목 vs 채우기 20건. 새 조사가 아니라 이미 걸어둔 refs 를 다시 읽는 일이다.
     원문 결과표에 두 군 숫자가 있다. 없으면 상대값(위험비·차이)이라도 적는다.
   - 영문 주어 20종 옮기기. 반드시 node tools/rename-subject.mjs 를 써라.
     subj 와 kinds.json 을 같이 바꿔야 해서, 손으로 고치면 빌드가 선다.
   - 그 밖의 경고(한 줄 길이, 번역투, 규모 숫자 겹침)도 20건 고친다.
     숫자는 건드리지 말고 문장만 다듬어라.

5. 커밋하고 푸시한다. 배치마다 한다. 몰아서 하지 마라.
   푸시가 곧 배포다. vercel 명령을 따로 치지 마라.

6. logs/state.json 을 갱신한다. 항목 수, 갈래별 수, 남은 일감, vs 밀린 건수,
   영문 주어 남은 종수, 다음에 이어갈 지점.
   세션이 중간에 끊겨도 이 파일만 읽으면 이어갈 수 있게 적어라.
   한 달을 도는 동안 대화 내용은 계속 압축돼 사라진다. 남는 기억은 이것뿐이다.

7. 일감 채우기 — 1번에서 30개 밑일 때만.
   node tools/expand-worklist.mjs --covered   로 이미 다룬 이름을 본다.
   거기 없는 이름 20개를 골라 인자로 넘긴 뒤 --write 로 덧붙인다.

   - 20개 중 15개는 약이 아닌 것으로 채운다. 보충제, 식품, 운동, 수면, 앱, 기기, 습관.
     ClinicalTrials.gov 에 개입 이름으로 등록된 말이어야 한다. 지어낸 말은 0건이 온다.
     인자 없이 그냥 돌리면 내장 씨앗 130개가 이미 그쪽으로 짜여 있다.
   - 항암제를 넣지 마라. 적응증이 잘게 쪼개져 있어서 한 물질에서 비슷한 줄만 쏟아진다.
   - 공백이 있는 이름은 따옴표로 묶어라. tranexamic acid 를 그냥 넘기면 acid 로
     잘린다. 지금까지 네 번 잘렸다.


## 막혔을 때 — 어느 경우에도 멈추지 마라

- 확장이 10개도 못 붙였다 → 다른 이름 20개로 한 번 더 돌린다. 두 번째도 10개 밑이면
  그만하고, 30개가 안 돼도 있는 것만 조사한다. 7번을 한 바퀴에 세 번 넘게 돌리지 마라.
  그게 제자리걸음이다.
- ClinicalTrials.gov 나 PubMed 가 500·타임아웃을 뱉는다 → 그 주제만 두 번까지 다시
  부른다. 세 번째도 실패하면 줄 끝에 — 조회 실패(날짜) 를 붙이고 [ ] 로 남겨둔 뒤
  다음 주제로 간다. 같은 요청을 계속 두드리지 마라.
- 빌드가 선다 → 방금 만든 항목 중 원인 파일을 고친다. 10분 안에 못 고치면 그 파일을
  지운다. 저장소를 깨진 채로 두고 다음 바퀴로 넘어가지 마라.
- 갈래를 모르겠다 → 찍지 마라. AGENTS.md 규칙 2.5 의 "누가 하는가" 기준으로 가르고,
  그래도 모르겠으면 그 주제를 건너뛰고 다른 것을 고른다. 틀린 갈래는 빈칸보다 나쁘다.
- 원문에 두 군 숫자가 없다 → 지어내지 마라. 상대값을 적고, 그것도 없으면 그 주제를
  [ ] 로 두고 다음으로 간다.
- 푸시가 거부된다 → git pull --rebase 하고 다시 민다. 충돌이 나면 내가 만든 항목
  파일만 살리고 상대 것은 그대로 둔다. 항목은 파일이 하나씩이라 안 겹친다.
- 두 바퀴 연속으로 항목을 하나도 못 만들었다 → 주제 고르는 갈래를 바꾼다. 같은 계열만
  파고 있다는 뜻이다. 무엇을 바꿨는지 logs/state.json 에 적어라.


## 여섯 바퀴마다 점검

숫자를 재서 logs/state.json 에 남긴다.

- 약국약·보충제·음식이 4분의 1을 넘었나 (지금 15%)
- 처방약 비중이 줄었나 (지금 38%)
- 효과 없음·해로운 쪽이 3분의 1 위인가 (지금 23%)
- vs 가 있는 항목이 늘었나 (지금 0%)
- 영문 주어가 줄었나 (지금 1,135종)
- 부작용이 효능이 된 항목이 늘었나 (지금 33건)
- 공개 사이트 항목 수가 로컬 빌드와 같은가

첫 번째가 여섯 바퀴 뒤에도 그대로면 7번에서 약이 아닌 것을 18개로 늘려라.
마지막이 다르면 vercel --prod 로 한 번 밀고, 왜 자동 배포가 안 걸렸는지
커밋 메시지에 적어라.


## 손대면 안 되는 것

- data/worklist.md 에서 끝난 줄을 지우거나 다른 파일로 옮기지 마라. 파일이 길다고
  정리하지 마라. expand-worklist.mjs 가 그 줄들을 보고 중복을 걸러낸다. 지우면
  중복이 쏟아진다.
- data/kinds.json 의 열쇠를 손으로 고치지 마라. tools/rename-subject.mjs 를 써라.
- site/ 를 직접 고치지 마라. 빌드 결과물이라 다음 빌드에 날아간다.
- 돈 드는 API 를 붙이지 마라. ClinicalTrials.gov 와 PubMed 는 키 없이 무료다.
- 지어내지 마라. 원문에 없는 숫자, 없는 DOI, 없는 NCT 번호를 쓰지 마라.


조사할 때 부작용이 효능이 된 사례를 특히 눈여겨봐라. 4,970건 중 33건이다.
안전성 검색에서 나온 이상반응이 누군가에게는 도움이 된 것, 1차 평가변수에서
실패했는데 다른 데서 신호가 난 것, 다른 병으로 그 약을 먹던 사람들의
동반질환이 같이 좋아진 것. claim 을 "무엇의 부작용을 무엇에 쓴다" 꼴로 써라.
```

---

## 여러 세션을 동시에 돌릴 때

항목을 파일 하나씩 나눠놨기 때문에 `data/entries/` 는 부딪히지 않습니다.
부딪히는 건 `data/worklist.md` 와 `data/kinds.json` 둘뿐이라, **세션마다 다른 갈래를
맡기면** 됩니다.

프롬프트의 2번을 이 줄로 바꾸세요.

```
data/worklist.md 에서 보충제·음식에 해당하는 줄만 골라라. 다른 갈래는 건드리지 마라.
```

갈래가 일곱이라 최대 일곱 세션까지 안전합니다. 그 이상은 `kinds.json` 에서 부딪힙니다.

---

## 끝나고 확인할 것

- `node build.mjs` 가 오류 없이 끝나는가
- 공개 사이트 항목 수가 로컬 빌드와 같은가 — https://effect-pool.vercel.app
- `logs/state.json` 의 숫자가 실제와 맞는가
