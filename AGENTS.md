# 근거 풀 (effect-pool)

**무엇을 했을 때 얼마나 달라지나.** 사람한테 실제로 재본 숫자를 한 줄씩 모으는 저장소다.

약만 담지 않는다. 오히려 약국약·보충제·음식·운동·잠·앱처럼 **사람들이 오늘 직접 해볼 수 있는 것**이
이 목록의 본론이다. 처방약은 어차피 의사가 정한다.

허가 밖 사용(오프라벨)만 담지도 않는다. 허가받은 용도든 아니든, 개입군과 대조군이
**얼마나 갈렸는지 숫자로 적을 수 있으면** 항목이 된다. 허가 여부는 버리는 기준이 아니라
읽는 사람이 참고할 라벨 하나다.

**이 저장소가 만드는 것이 아닌 것**
- 치료법 추천 목록이 아니다
- 복용 안내가 아니다
- "효과 있는 것 모음"이 아니다. 효과 없다고 나온 것과 해로웠던 것을 같은 비중으로 담는다

---

## 파일 구조

```
data/worklist.md      조사할 주제 목록. 여기서 골라서 시작한다
data/entries/*.json   항목 하나가 파일 하나. 파일명은 반드시 <id>.json
data/kinds.json       주어 이름 -> 갈래. 항목이 아니라 주어 단위로 한 줄씩
data/meta.json        확인 수준 다섯 칸과 갈래 일곱 개 정의
src/template.html     화면 틀. __ENTRIES__ 자리에 데이터를 끼워 넣는다
build.mjs             data + template -> site/index.html
site/index.html       빌드 결과물. 직접 고치지 마라. 다음 빌드에 날아간다
```

**항목을 파일 하나씩 나눠둔 이유는 여러 세션이 동시에 채워도 안 깨지게 하려는 것이다.**
한 파일에 몰아 쓰지 마라. 새 항목은 새 파일을 만든다.
`updated` 날짜는 각 항목의 `queried.date` 중 가장 나중 것으로 빌드가 자동으로 정한다.

빌드: `node build.mjs`
배포: `vercel --prod`

---

## 항목 하나의 모양

```json
{
  "id": "fenbendazole-cancer-case",
  "subj": "펜벤다졸",
  "claim": "말기암이 나았다",
  "line": "혼자 한 달 먹은 환자한테 심한 간 손상이 왔습니다",
  "effect": "간효소 정상 상한 20배",
  "vs": "대조군 없는 단일 증례. 복용 전 정상이던 간효소가 한 달 뒤 정상 상한의 20배로 올랐다",
  "tier": "case",
  "dir": "harm",
  "n": 1,
  "synth": "single",
  "known": "off",
  "knownWhy": "암 치료로 허가받은 적 없음",
  "why": "...",
  "saw": "...",
  "limit": "...",
  "against": "...",
  "use": "...",
  "refs": [
    { "label": "Yamaguchi 2021 · Case Rep Oncol · 증례", "doi": "10.1159/000516276", "pmid": "34248555" }
  ],
  "queried": {
    "date": "2026-08-04",
    "ctgov": "intervention=fenbendazole → 7건 반환, 물질 대조 후 0건",
    "pubmed": "fenbendazole cancer antitumor → 21건",
    "negative": "fenbendazole no effect tumor → 1건 (Duan 2013)",
    "safety": "fenbendazole hepatotoxicity case report → 1건"
  }
}
```

필드 설명은 아래 규칙과 함께 읽어라.

---

## 규칙 1 — 확인 수준(tier)을 정하는 법

다섯 칸뿐이다. 위에서부터 내려오면서 **처음 해당하는 칸**에 넣는다.

| 값 | 뜻 | 판정 기준 |
|---|---|---|
| `trial` | 사람한테 직접 나눠서 줘봄 | 사람을 대상으로 배정해서 투여·시행한 연구가 있다. 무작위배정 시험, 단일군 1상 모두 포함 |
| `obs` | 사람을 지켜보기만 함 | 사람 데이터는 있는데 지켜본 것뿐이다. 코호트, 환자대조군, 단면조사 |
| `animal` | 동물까지만 해봄 | 동물 실험까지만 있다 |
| `lab` | 세포나 시험관까지만 | 세포·시험관 실험까지만 있다 |
| `case` | 한 명 이야기 | 개별 사례 보고, 일화, 체험담 |

**가장 자주 틀리는 지점이 여기다.** 칸을 정할 때 기준은 "그 물질에 사람 연구가 있느냐"가 아니라
**"이 주장에 대해 사람 연구가 있느냐"**다.

펜벤다졸은 동물 구충제로 승인돼 있고 동물 안전성 자료가 많다.
그런데 "항암"이라는 주장에 대한 사람 연구는 0건이다. → `animal`이지 `trial`이 아니다.

메벤다졸은 사람 구충제로 허가돼 있다. 그것만으로는 `trial`이 아니다.
뇌종양 환자에게 실제로 투여한 1상이 있어야 `trial`이다. 그게 있으니 `trial`이다.

애매하면 **낮은 칸**에 넣는다. 올리는 건 나중에 근거가 늘면 하면 된다.

---

## 규칙 2 — 방향(dir)을 정하는 법

| 값 | 표시 | 뜻 |
|---|---|---|
| `pos` | ● | 그 칸에서 효과나 연관을 확인했다 |
| `open` | ○ | 연구가 돌아가는 중이고 결과가 아직 없다 |
| `null` | ✕ | 효과가 없었거나, 보정하니 연관이 사라졌다 |
| `harm` | ⚠ | 해로운 쪽 신호가 나왔다 |

`null`과 `harm`이 전체의 3분의 1 아래로 떨어지면 조사가 한쪽만 보고 있다는 뜻이다.
그때는 새 항목을 추가하지 말고 규칙 3의 5·6번을 다시 돌려라.

`harm`이라고 목록에서 위로 올리거나 따로 빼지 마라. 자기 확인 수준 칸에 그대로 둔다.
읽는 사람이 판단할 자료이지 경고문이 아니다. ⚠ 표시만 붙이면 된다.

---

## 규칙 2.5 — 갈래(kind)를 정하는 법

**이 목록에서 제일 먼저 갈라야 하는 것이다.** 갈래가 없으면 5천 줄이 통째로 약 이야기로 보인다.

갈래는 항목이 아니라 **주어에 딸린 성질**이다. 그래서 `data/entries/*.json` 이 아니라
`data/kinds.json` 에 `"주어 이름": "갈래"` 로 한 줄씩 적는다. 같은 주어를 쓰는 항목이
몇 개든 한 줄만 적으면 다 붙는다.

| 값 | 이름 | 무엇이 들어가나 |
|---|---|---|
| `otc` | 약국약 | 처방 없이 약국에서 사는 약. 진통제, 감기약, 제산제, 연고 |
| `supp` | 보충제 | 비타민, 미네랄, 건강기능식품, 허브 추출물, 프로바이오틱스 |
| `food` | 음식 | 먹는 것과 먹는 방식. 커피, 견과, 지중해식, 간헐적 단식 |
| `move` | 운동 | 몸을 쓰는 것. 걷기, 근력운동, 요가, 타이치, 재활 |
| `life` | 생활·마음 | 잠, 금연, 명상, 상담, 심리·행동 프로그램, 사회적 지지 |
| `care` | 시술·기기 | 받는 처치와 쓰는 장비. 침, 물리치료, 앱, 웨어러블, 전기자극, 수술 |
| `rx` | 처방약 | 의사가 처방하는 약 |

- 새 주어를 쓰면 `kinds.json` 에 줄을 먼저 넣어라. **없으면 빌드가 선다**
- 애매하면 "누가 하는가"로 가른다. 내가 하면 `life`, 남이 해주거나 기계가 하면 `care`
- 요리를 **배우는** 프로그램은 `life`, 음식을 **받는** 것은 `food`
- 성분 하나로 뽑아 캡슐로 먹으면 `supp`, 음식 그대로 먹으면 `food`
  (아마씨유 보충제는 `supp`, 아마씨를 먹는 건 `food`)
- 자동으로 붙이려면 `node tools/classify-kind.mjs --write`. 규칙이 확실할 때만 붙고
  나머지는 비워둔다. **찍어서 붙이지 마라. 틀린 갈래는 빈칸보다 나쁘다**

### 갈래가 한쪽으로 쏠리면 목록이 쓸 데가 없어진다

빌드가 **약국약·보충제·음식을 합쳐 4분의 1이 안 되면** 경고한다.
그 경고가 뜨면 다음 일감을 그 세 갈래에서 뽑아라. 처방약을 더 넣지 마라.

---

## 규칙 2.6 — 주어 이름은 한국어로

화면 맨 앞에 서는 글자다. `myofascial release` 라고 적혀 있으면 한국어 목록이 아니게 된다.

- 새 항목의 주어에 소문자 영문이 4글자 넘게 들어가면 **빌드가 선다**
- 대문자 약어는 그대로 둔다. `EGCG`, `HMB`, `CPAP` 은 옮길 말이 없다
- 이미 들어간 영문 이름을 옮길 때는 **반드시 도구를 써라.**
  `subj` 와 `kinds.json` 열쇠를 같이 바꿔야 하는데, 하나만 고치면 빌드가 선다

```bash
node tools/rename-subject.mjs "myofascial release" "근막이완"
node tools/rename-subject.mjs --file rename.tsv     # 한 줄에 "옛 이름<탭>새 이름"
```

`id` 와 파일명은 영문 그대로 둔다. 주소로 쓰는 값이라 바꾸면 걸어둔 링크가 죽는다.

---

## 규칙 3 — 항목 하나를 조사하는 순서

이 순서를 바꾸지 마라. 특히 5·6번을 건너뛰면 항목이 완성된 게 아니다.

**1. 주장을 한 문장으로 확정한다**
"무엇이 무엇에 대해"까지 적는다. "펜벤다졸이 좋다"가 아니라 "펜벤다졸이 말기암을 낫게 한다".
이게 흐리면 뒤가 전부 흐려진다.

**2. 사람 개입 연구를 찾는다** — ClinicalTrials.gov API
```
https://clinicaltrials.gov/api/v2/studies?query.intr=<물질>&query.cond=<대상>&countTotal=true
```

**3. 물질 동일성을 대조한다** ← 건너뛰지 마라
반환된 각 건의 `interventions` 배열에 대상 물질 이름이 실제로 들어 있는지 문자열로 확인한다.
검색 엔진이 동의어를 확장해서 **다른 물질을 끼워 넣는다.**
펜벤다졸로 조회했더니 옥스펜다졸 3건과 무관한 시험 4건이 딸려 왔다. 실제 펜벤다졸 시험은 0건이었다.
대조를 안 하면 "사람 시험 7건"으로 기록된다.

**4. 사람 관찰 연구를 찾는다** — PubMed

**5. 반대쪽을 따로 찾는다** ← 건너뛰지 마라
긍정 검색어로 나온 목록에 음성 결과가 섞여 들어오길 기대하지 마라. 안 들어온다.
따로 이런 말로 돌린다: `no effect`, `did not`, `failed to`, `negative`, `null association`, `no significant difference`
펜벤다졸 음성 결과(Yale, 2013)는 이 단계에서만 나온다. 이게 빠지면 그림이 뒤집힌다.

**6. 안전성을 따로 찾는다** ← 건너뛰지 마라
`adverse`, `toxicity`, `hepatotoxicity`, `case report`, `self-medication`
효능만 모으면 펜벤다졸 간 손상 증례가 사라진다.

**7. tier와 dir을 정한다** (규칙 1, 2)

**8. 한 줄을 쓴다** (규칙 4)

**9. 나머지 칸을 채운다**
- `why` — 왜 이 칸에 넣었는지. 판정 근거를 여기 남긴다
- `saw` — 무슨 연구인지. 설계, 인원, 대상
- `limit` — 믿을 때 조심할 점. 교란, 대리지표, 표본 크기
- `against` — 반대쪽 결과. 없으면 `"—"`가 아니라 `"찾아봤지만 없었습니다"`라고 적는다
- `use` — 그래서 뭐가 달라지는지
- `refs` — 원문으로 넘어갈 자료. 아래 규칙 4를 따른다

**10. `queried`에 조회 기록을 남긴다** ← 건너뛰지 마라
날짜와 실제 검색어, 반환 건수를 적는다.
이게 없으면 나중에 **"0건"과 "안 찾아봤음"을 구분할 방법이 없다.**

---

## 규칙 3.5 — 근거의 무게 (`n`, `synth`)

확인 수준만 보면 **36명짜리 시험과 61,589명 메타분석이 같은 칸에 온다.** 그래서 규모를 따로 받는다.

| 필드 | 값 | 뜻 |
|---|---|---|
| `n` | 정수 또는 `null` | 그 근거에 들어간 **사람 수**. 가장 큰 수를 적는다 |
| `synth` | `"single"` / `"meta"` | 연구 하나냐, 여러 연구를 합친 것이냐 |

**둘 다 필수다. 없으면 빌드가 선다.**

- 메타분석이면 합친 총 인원을 적는다. 시험 개수가 아니라 사람 수다
- 동물·세포 연구는 사람이 없으니 `n: null`, `synth: "single"`
- 증례 보고는 `n: 1`
- 원문에서 사람 수를 못 찾으면 `null` 로 두되, **문장에도 규모를 적어라.** 읽는 사람이 무게를 재야 한다

`synth: "single"` 이면서 `n` 이 50 미만이면 화면에 붉게 표시된다. 버리라는 뜻이 아니라
"이것 하나로 판단하지 마라"는 표시다. 그대로 두고 `limit` 에 규모가 작다는 점을 적어라.

문장에서 정규식으로 뽑는 방식은 쓰지 않는다. 118건에 해봤더니 43건을 놓쳤다.

---

## 규칙 3.55 — 얼마나 갈렸나 (`vs`, `effect`)

**이 목록이 대답하려는 것이 이 한 줄이다.** "좋아졌다"만 적힌 항목은 읽어도 남는 게 없다.

| 필드 | 길이 | 무엇을 적나 |
|---|---|---|
| `vs` | 한 문장 | 무엇을 재서 개입군 얼마, 대조군 얼마, 차이 얼마. **숫자가 없으면 빌드가 선다** |
| `effect` | 26자 안쪽 | 목록 줄 앞에 붙는 한 조각. `vs` 에서 제일 중요한 숫자만 뽑는다 |

```json
"vs": "12주 뒤 다모 점수가 개입군 8.4점, 위약군 14.5점으로 6.1점 갈렸다",
"effect": "다모 점수 42% 감소"
```

- **대조군이 없으면 없다고 적어라.** `"대조군 없는 단일군. 전 32.1점 → 후 24.8점"`
- 상대값만 있으면 상대값을 적는다. `"주요 출혈 위험비 0.31 (95% CI 0.18~0.54)"`
- 원문에 숫자가 있는데 못 찾겠으면 **찾을 때까지 찾아라.** 이건 새 조사가 아니라
  이미 연 논문의 결과표를 다시 보는 일이다
- `dir` 이 `open`(진행 중)이면 결과가 없으니 `vs` 를 요구하지 않는다
- 지어내지 마라. 원문에 없는 숫자를 쓰느니 항목을 만들지 마라

**옛 항목에 `vs` 가 비어 있다.** 요구하기 전에 만든 것들이라 빌드가 건수만 세서 알려준다.
한 바퀴에 20건씩 채워라. 새 조사가 아니라 이미 걸어둔 `refs` 를 다시 읽는 일이다.

---

## 규칙 3.6 — 허가 여부 (`known`, `knownWhy`)

**버리는 기준이 아니다.** 허가받은 용도여도 항목으로 만든다. 사람들이 궁금해하는 건
"얼마나 달라지나"이지 "허가가 났나"가 아니다. 미녹시딜이 머리를 얼마나 나게 하는지는
허가가 났어도 알 값어치가 있다.

다만 다 아는 얘기가 목록 맨 위를 통째로 먹지는 않게 순위에서 조금 내린다.
그것뿐이다. 조사를 건너뛰는 이유로 쓰지 마라.

옛 규칙은 허가 적응증이면 조사를 멈추라고 했다. 그래서 8,877줄을 처리하는 동안
2,559줄을 허가 적응증이라는 이유로 버렸다. **그 줄들은 이제 버리지 않는다.**

| 값 | 뜻 |
|---|---|
| `"label"` | 어느 나라에서든 그 용도로 **허가받았다**. 오프라벨이 아니다 |
| `"standard"` | 허가는 없지만 진료지침에 실린 표준 요법이다. 오프라벨이지만 새롭지 않다 |
| `"off"` | 허가 밖이고 표준도 아니다. **이 목록이 찾는 것** |

`knownWhy` 에 근거를 한 조각 적는다 — `"라티세 2008 속눈썹 허가"`, `"틱 진료지침 1차 약"`.
`known` 을 적었는데 `knownWhy` 가 비면 빌드가 선다.

- 규칙 3의 1단계에서 허가사항을 어차피 확인한다. 그때 본 걸 그대로 적으면 된다
- 모르겠으면 **비워둬라.** 찍지 마라. 빈 값은 점수를 깎지도 올리지도 않는다
- 나라마다 다르면 넓게 본다. 유럽만 허가라도 `"label"` 이고 `knownWhy` 에 어디인지 적는다
- 같은 물질이라도 용도마다 다르다. 탈리도마이드는 HIV 구강궤양이 `label`,
  소화관 혈관기형 출혈이 `off` 다

**위쪽 80건에 `known` 이 없으면 빌드가 경고한다.** 위에 서는 항목부터 채워라.

### 부작용이 효능이 된 사례를 따로 찾아라

"이 약에 이런 효능이 있었어?" 하는 얘기가 이 목록에서 제일 값어치 있다.
그런데 716건 중 4건뿐이고 그중 둘(미녹시딜-탈모, 비마토프로스트-속눈썹)은 이미 허가받았다.
**아직 허가 안 난 것**을 찾아야 한다. 조사할 때 이런 실마리를 놓치지 마라.

- 안전성 검색에서 나온 이상반응이 **누군가에게는 도움이 된 것**
- 시험이 1차 평가변수에서 실패했는데 **다른 데서 신호가 난 것**
- 다른 병으로 그 약을 먹던 사람들의 **동반질환이 같이 좋아진 것**

---

## 규칙 4 — 근거 자료 다는 법

`refs`는 배열이고 **비면 빌드가 선다.** 읽는 사람이 원문으로 못 넘어가면 항목이 아니다.

각 항목은 `label` 하나와 **`doi` · `pmid` · `pmc` · `nct` 중 최소 하나**를 가진다.
화면에서 이 값들이 링크로 바뀐다.

| 키 | 형식 | 링크 |
|---|---|---|
| `doi` | `10.####/…` | `https://doi.org/{doi}` |
| `pmid` | 숫자만 | `https://pubmed.ncbi.nlm.nih.gov/{pmid}/` |
| `pmc` | `PMC#####` | `https://pmc.ncbi.nlm.nih.gov/articles/{pmc}/` |
| `nct` | `NCT########` | `https://clinicaltrials.gov/study/{nct}` |

`label`은 사람이 읽을 이름이다. `저자 연도 · 학술지 · 성격` 꼴로 적는다.
성격에 `음성 결과`, `증례`, `전임상`, `모집 중`, `중단` 같은 말을 넣으면 펼치지 않고도 구성이 보인다.

DOI가 없는 옛 논문은 `pmc`나 `pmid`만 적으면 된다. 없는 DOI를 지어내지 마라. 빌드가 형식을 검사한다.

**반대쪽 근거도 `refs`에 넣어라.** 음성 결과 논문을 본문에서만 언급하고 링크를 안 걸면
읽는 사람이 확인할 방법이 없다.

---

## 규칙 5 — 한 줄 쓰는 법

화면은 `주어 — 한 줄` 로 붙여서 보여준다. 그러니 `line` 에는 **주어를 빼고** 뒷부분만 적는다.
주어로 시작하면 "커피 — 커피 — …" 이 되므로 빌드가 선다.

아래 예시는 붙여놓은 결과다.

**글자 수를 세지 마라.** 한 줄에 들어가면 된다. 핵심 문장을 온전히 적는 게 먼저다.
줄이려다 뜻이 뭉개지면 잘못 줄인 거다.

지킬 것:

- **결론을 적는다. 주제를 적지 않는다.**
  - 나쁨: `카페인 — 눈물 분비와의 관계`
  - 좋음: `카페인 — 건강한 성인 78명한테 줬더니 눈물이 오히려 조금 늘었습니다`

- **능동형으로 쓴다.**
  - 나쁨: `~가 확인됐습니다`, `~로 보고됐습니다`, `~가 등록돼 있습니다`
  - 좋음: `~를 확인했습니다`, `연구진이 ~를 보고했습니다`, `~시험이 있습니다`

- **숫자를 넣는다.** "많은 환자"가 아니라 "78명".

- **번역투를 쓰지 않는다.**
  - 쓰지 말 것: `현재 존재하는`, `~라는 점이 ~를 시사합니다`, `~에 대한 근거를 제공합니다`,
    `~의 가능성이 제기됩니다`, `주목할 만한`, `~로 나타났습니다`
  - 관형절을 겹치지 마라. 짧게 끊어라.

- **읽는 사람이 아는 말로 쓴다.** `tear meniscus height`가 아니라 `눈물`.
  정확한 지표 이름은 `saw` 칸에 적으면 된다.

---

## 규칙 6 — 쓰면 안 되는 것

- 용량, 복용법, 복용 기간
- 권하는 말 (`~해보세요`, `~에 좋습니다`, `추천`)
- 확인 수준을 확률이나 퍼센트로 바꾸는 것. 칸 이름 그대로 쓴다
- 출처 없는 문장
- 사람 시험이 없는 항목을 `trial`로 올리는 것

---

## 하루 배치로 돌릴 때

1. `data/worklist.md`에서 아직 `[ ]`인 줄을 고른다. 주제를 새로 지어내지 마라
2. 한 번에 한 물질, 한 주장. 물질 하나에 주장이 여러 개면 항목도 여러 개다
3. `id`는 `물질-대상-성격` 꼴 (`mebendazole-glioma-phase1`). 파일명은 `<id>.json`
4. 다 적었으면 `data/entries/<id>.json`을 만들고 worklist의 그 줄을 `[x]`로 바꾼다
5. `node build.mjs`가 통과해야 끝난 것이다. 빌드가 서면 고치고 다시 돌린다
6. 커밋은 5~10건 단위로. 메시지에 채운 주제를 적는다

### "근거 없음"으로 뺄 때 — 두 경우를 구분해라

이걸 헷갈리면 좋은 항목을 버린다. 실제로 한 번 버렸다.

**항목이 되는 경우 — 누군가 찾아봤고 없더라고 적어놨다.**
검토 논문이나 메타분석이 "무작위 시험이 없다", "근거가 부족하다"고 결론냈다면 그건 근거다.
링크를 걸 수 있고 `null`로 판정할 수 있다. **아주 좋은 항목이니 반드시 만들어라.**
예: 디톡스 식단은 2015년 비판적 검토가 "상업용 디톡스 식단의 효과를 본 무작위 대조시험은
하나도 없다"고 적었다. 이건 `obs` + `null` 항목이지 버릴 주제가 아니다.

**주장과 결과가 어긋나 보여도 버리지 마라.**
주장이 "체중이 는다"인데 시험은 배고픔과 에너지 소비만 쟀다면, 그건 항목으로 만들고
`limit`에 "체중을 직접 재지 않았다"를 적으면 된다. 앞단 지표만 쟀다는 이유로 버리지 마라.

**진짜로 뺄 경우 — 검색해도 아무것도 안 나온다.**
검토도 없고 원저도 없을 때만이다. worklist 줄을 `[x]`로 바꾸고
줄 끝에 `— 근거 없음(YYYY-MM-DD 조회, 검색어)`을 붙인다. 검색어까지 남겨야
다음 세션이 같은 검색을 반복하지 않고 다른 각도로 판다.

### 자동 추출된 주제를 다룰 때

`## 자동 추출` 아래 줄들은 `tools/expand-worklist.mjs`가 ClinicalTrials.gov에서 뽑은 후보다.
질환명이 영문이고 **허가 적응증이 섞여 있다.**

- **허가 적응증이라고 버리지 마라.** 옛 규칙이었고 지금은 아니다. 허가 안이든 밖이든
  개입군과 대조군이 갈린 숫자가 있으면 항목이 된다. `known` 에 `label` 이라고 적으면 된다
- `subj` 와 `claim` 은 한국어로 다시 쓴다
  (`metformin — Colon Cancer에 듣는다` → 주어 `메트포르민`, 주장 `당뇨약이 대장암에도 듣는다`)

### 주제가 떨어지면

```bash
node tools/expand-worklist.mjs --write            # 내장 물질 목록
node tools/expand-worklist.mjs sertraline --write # 물질을 직접 지정
```

이미 worklist나 `data/entries/`에 있는 조합은 알아서 빠진다. 주제를 손으로 지어내지 마라.

### 배치 끝나고 매번 확인할 것

- [ ] 새 항목 전부 `queried`가 채워져 있나
- [ ] 새 항목 전부 물질 동일성을 대조했나
- [ ] 새 항목 전부 `vs`에 개입군·대조군 숫자가 들어갔나
- [ ] 새 주어가 `kinds.json`에 있고, 갈래가 맞나
- [ ] 새 주어 이름이 한국어인가
- [ ] `refs` 링크를 실제로 열어봤나 (죽은 DOI·잘못된 NCT 번호)
- [ ] 반대쪽 근거도 `refs`에 링크로 들어갔나
- [ ] `null` + `harm` 비중이 3분의 1 이상인가
- [ ] 약국약·보충제·음식이 4분의 1 이상인가
- [ ] `against`가 `"—"`로 남은 항목이 없나
- [ ] 한 줄에 번역투가 섞이지 않았나

---

## 화면 디자인

기준은 `G:\내 드라이브\여형준님\31 PPT 디자인 키트\AGENTS.md`다. 화면을 손볼 때 먼저 읽어라.

지금 이 사이트가 그 키트에서 가져온 것:

- 조용한 배경 위 흰 라운드 카드, 넉넉한 여백. 팔레트는 키트의 시안 계열(`#0E7490`)
- 아이콘을 쓰지 않는다. 눈금과 글자로 말한다
- 강조색은 하나만. 해로운 쪽 표시(`#B4593F`)만 예외로 두고, 그 외에는 전부 무채색
- 큰 숫자 + 작은 설명으로 요약을 보여준다
- 폰트는 국문 Pretendard, 숫자·라벨 Inter. **Inter에 한글을 넣으면 글자가 사라진다** — 숫자 전용으로만 쓴다

눈금이 두 가지를 동시에 말한다. 이 규칙을 깨지 마라.

- **길이** = 얼마나 확인했나 (확인 수준 1~5칸)
- **색** = 뭐가 나왔나 (확인함·효과 없음·해로운 쪽·진행 중)

갈래는 눈금에 섞지 않는다. 갈래마다 색을 다르게 주면 눈금 색과 싸워서 둘 다 안 읽힌다.
회색 라벨 하나와 위쪽 칩으로만 보여준다.

목록 순서는 점수대로 세운 다음 자리를 흔들어서 **같은 물질과 같은 갈래가 붙어 있지 않게** 한다.
그냥 두면 처방약이 첫 화면을 다 덮는다. 점수 자체는 건드리지 않는다.

그래서 "사람한테 제대로 써봤는데 효과가 없었다"가 5칸 회색으로, "한 명 이야기인데 해로웠다"가
1칸 붉은색으로 나온다. 길이와 색을 한 축으로 합치지 마라. 정보가 사라진다.

---

## 조회에 쓰는 API

**브라우저를 띄우지 마라.** 둘 다 인증이 없는 REST 라 `curl` 하나로 끝난다.

### ClinicalTrials.gov v2

```bash
curl -s "https://clinicaltrials.gov/api/v2/studies?query.intr=<물질>&query.cond=<대상>&countTotal=true&pageSize=50&fields=NCTId,BriefTitle,Phase,OverallStatus,EnrollmentCount,Condition,InterventionName"
```

`totalCount` 가 전체 건수다. `interventions[].name` 이 물질 동일성 대조에 쓸 필드다.

**반환된 건수를 그대로 믿지 마라.** 검색이 동의어를 확장해서 다른 물질을 끼워 넣는다.
`InterventionName` 에 조회한 물질 이름이 실제로 들어 있는 건만 세라.

```bash
# 예: 펜벤다졸 -> 7건 반환되지만 실제로는 0건 (옥스펜다졸 3건 + 무관 4건)
curl -s "https://clinicaltrials.gov/api/v2/studies?query.intr=fenbendazole&countTotal=true&pageSize=50&fields=NCTId,InterventionName" \
  | grep -ci "fenbendazole"
```

### PubMed E-utilities

```bash
# 검색 -> PMID 목록과 총 건수
curl -s "https://eutils.ncbi.nlm.nih.gov/entrez/eutils/esearch.fcgi?db=pubmed&term=<검색식>&retmode=json&retmax=20"

# PMID -> 제목·저널·연도·DOI
curl -s "https://eutils.ncbi.nlm.nih.gov/entrez/eutils/esummary.fcgi?db=pubmed&id=<PMID,PMID>&retmode=json"
```

키 없이 초당 3회까지다. 그 이상 때리면 429 가 온다. 호출 사이에 0.4초쯤 쉬어라.
`esummary` 의 `articleids` 에서 `doi` 와 `pmc` 를 꺼내 `refs` 에 넣는다. 없으면 `pmid` 만 넣는다.

### 반대쪽·안전성 검색식 틀

규칙 3의 5·6번에서 쓴다. 긍정 검색어로 나온 목록에 이게 섞여 들어오길 기대하지 마라.

```
<물질> AND <대상> AND (no effect OR "did not" OR "failed to" OR negative OR "no significant difference")
<물질> AND (adverse OR toxicity OR hepatotoxicity OR "case report" OR overdose OR "self-medication")
```

---

## 출처 우선순위

1. **ClinicalTrials.gov** — 사람 개입 연구의 존재 여부는 여기가 1순위다.
   2차 결과변수가 구조화된 필드라 파싱할 필요가 없고, 등록 시점에 미리 선언되므로
   나중에 논문이 보고한 것과 대조하면 사후 선택을 잡아낼 수 있다.
   전수가 필요하면 API 대신 AACT를 쓴다 (매일 갱신, 무료).
2. **PubMed** — 관찰 연구, 전임상, 증례. E-utilities API.
3. **의약품안전나라 / openFDA SPL** — 허가사항. "오프라벨"을 판정하려면 기준선이 필요하다.
4. **심평원 허가초과 사용약제 승인 목록** — 국내에서 이미 심의를 거친 건들. 승인·불승인이 곧 라벨이다.

SemMedDB는 2024년 12월로 유지보수가 끝났다. 과거 구간 baseline으로만 쓰고 이후는 직접 돌려야 한다.
